spring:
  application:
    name: GatewayServer # 网关服务名（Nacos注册名）
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848 # Nacos服务地址
        username: nacos # 默认用户名
        password: nacos # 默认密码